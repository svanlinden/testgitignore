'use strict';
const fs = require("fs");
const persons = require("../../persons.json");

function findBy(carsTest) {


}

function carsTest(subEntiteit) {
    if (subEntiteit === persons.cars.id){

    }
}

persons.cars.forEach(element => console.log(element.id));